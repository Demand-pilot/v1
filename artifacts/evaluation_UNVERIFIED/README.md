These artifacts were not reproducibly generated. The script that claims to produce them
(`scripts/train_and_evaluate_all_origins.py`) raises `NameError` on import. Superseded by
`artifacts/evaluation/` once Phase 4 completes. Do not cite these numbers.
